namespace DetectorAnimal.Domain.AccountManager
{
    public enum StatusCodeAccount
    {
        EmailAlreadyRegistered,
        InvalidUserData,
        UserNotVerified,
        Failed,
        OK
    }
}